spring:
  datasource:
    url: jdbc:mysql://shortline.proxy.rlwy.net:42939/railway
    username: root
    password: WFEAZovufzrUiPHIiTkCulsSANYjqxDv
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 해커톤에서는 보통 update/create
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect

openai:
  api:
    key: ${OPENAI_API_KEY}   # 환경변수에서 가져오기
  model: gpt-4o-mini         # 모델명도 함께 관리 가능
